# 九、渠道与消息系统

渠道系统让 CountBot 能够同时连接多个聊天平台。消息系统提供可靠的消息投递保障。

## 1、渠道系统架构

```
                    ┌─────────────────────────┐
                    │    ChannelManager        │
                    │  频道管理器（统一管理）     │
                    └─────────┬───────────────┘
                              │
        ┌─────────┬───────────┼───────────┬──────────┐
        ▼         ▼           ▼           ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
   │ Feishu │ │DingTalk│ │  QQ    │ │Telegram│ │ WeChat │
   │ 飞书   │ │ 钉钉   │ │  QQ   │ │Telegram│ │ 微信   │
   └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘
       │          │          │          │          │
       └──────────┴──────────┴──────────┴──────────┘
                              │
                              ▼
                   EnterpriseMessageQueue
                      (企业级消息队列)
                              │
                              ▼
                   ChannelMessageHandler
                     (频道消息处理器)
```

## 2、BaseChannel（频道基类）

核心文件：`backend/modules/channels/base.py`

### （1）数据结构

```python
@dataclass
class InboundMessage:
    """入站消息 - 从聊天平台接收"""
    channel: str           # 渠道名（如 "telegram"）
    sender_id: str         # 发送者 ID
    chat_id: str           # 聊天 ID
    content: str           # 消息内容
    media: list[str] = []  # 媒体文件路径列表
    metadata: dict = {}    # 元数据

@dataclass
class OutboundMessage:
    """出站消息 - 发送到聊天平台"""
    channel: str           # 目标渠道
    chat_id: str           # 目标聊天 ID
    content: str           # 消息内容
    media: list[str] = []  # 媒体文件列表
    metadata: dict = {}    # 元数据
```

所有渠道都使用统一的消息格式，这是多渠道统一的基础。

### （2）抽象接口

```python
class BaseChannel(ABC):
    name: str = "base"

    def __init__(self, config):
        self.config = config
        self._running = False
        self._message_callback = None

    @abstractmethod
    async def start(self) -> None:
        """启动频道"""

    @abstractmethod
    async def stop(self) -> None:
        """停止频道"""

    @abstractmethod
    async def send(self, msg: OutboundMessage) -> None:
        """发送消息"""

    @abstractmethod
    async def test_connection(self) -> dict:
        """测试连接"""
```

### （3）权限校验

```python
# base.py:98-114
def is_allowed(self, sender_id):
    """检查发送者是否在白名单中"""
    allow_list = getattr(self.config, "allow_from", [])
    if not allow_list:
        return True  # 空白名单 = 允许所有人

    sender_str = str(sender_id)
    if sender_str in allow_list:
        return True

    # 支持 "id|username" 复合格式
    if "|" in sender_str:
        for part in sender_str.split("|"):
            if part and part in allow_list:
                return True
    return False
```

### （4）入站消息处理

```python
# base.py:120-148
async def _handle_message(self, sender_id, chat_id, content, media=None, metadata=None):
    """处理入站消息：校验权限后转发到回调"""
    # 1. 权限检查
    if not self.is_allowed(sender_id):
        logger.warning(f"Access denied for {sender_id}")
        return

    # 2. 创建统一格式的消息对象
    msg = InboundMessage(
        channel=self.name,
        sender_id=str(sender_id),
        chat_id=str(chat_id),
        content=content,
        media=media or [],
        metadata=metadata or {},
    )

    # 3. 通过回调函数转发给 ChannelManager
    if self._message_callback:
        await self._message_callback(msg)
```

## 3、ChannelManager（频道管理器）

核心文件：`backend/modules/channels/manager.py`

### （1）频道注册表

```python
# manager.py:16-23
_CHANNEL_REGISTRY = {
    "telegram": ("backend.modules.channels.telegram", "TelegramChannel"),
    "discord":  ("backend.modules.channels.discord",  "DiscordChannel"),
    "qq":       ("backend.modules.channels.qq",       "QQChannel"),
    "wechat":   ("backend.modules.channels.wechat",   "WeChatChannel"),
    "dingtalk":  ("backend.modules.channels.dingtalk",  "DingTalkChannel"),
    "feishu":   ("backend.modules.channels.feishu",   "FeishuChannel"),
}
```

使用 `(模块路径, 类名)` 元组进行延迟导入，未启用的渠道不会被加载。

### （2）频道初始化

```python
# manager.py:47-71
def _init_channels(self):
    for name, (module_path, class_name) in _CHANNEL_REGISTRY.items():
        channel_cfg = getattr(channels_config, name, None)
        if not channel_cfg or not getattr(channel_cfg, "enabled", False):
            continue  # 未启用则跳过

        # 动态导入并实例化
        module = __import__(module_path, fromlist=[class_name])
        cls = getattr(module, class_name)
        self.channels[name] = cls(channel_cfg)

    # 为所有频道设置统一的消息回调
    for channel in self.channels.values():
        channel.set_message_callback(self._on_inbound_message)
```

### （3）频道监督与自动重连

```python
# manager.py:104-133
async def _start_channel_supervised(self, name, channel):
    """监督循环 - 异常退出后自动重连（指数退避）"""
    initial_backoff = 5     # 初始等待 5 秒
    max_backoff = 300       # 最大等待 300 秒（5 分钟）
    backoff = initial_backoff

    while self._running:
        start_time = asyncio.get_event_loop().time()
        try:
            await channel.start()   # 启动频道
        except asyncio.CancelledError:
            break
        except Exception as e:
            logger.error(f"Channel {name} error: {e}")

        if not self._running:
            break

        # 如果运行超过 60 秒才断开，重置退避时间
        elapsed = asyncio.get_event_loop().time() - start_time
        if elapsed > 60:
            backoff = initial_backoff

        # 指数退避重连
        await asyncio.sleep(backoff)
        backoff = min(backoff * 2, max_backoff)  # 5 → 10 → 20 → 40 → ... → 300
```

这个设计非常巧妙：
- 如果频道正常运行了很长时间后断开（如网络抖动），退避时间重置为 5 秒，快速重连
- 如果频道反复快速崩溃（如配置错误），退避时间逐步增长，避免频繁重试

### （4）消息路由

```python
# 入站消息路由：频道 → 消息总线
async def _on_inbound_message(self, msg: InboundMessage):
    await self.bus.publish_inbound(msg)

# 出站消息路由：消息总线 → 频道
async def _dispatch_outbound(self):
    while self._running:
        msg = await self.bus.consume_outbound()
        channel = self.channels.get(msg.channel)
        if channel:
            await channel.send(msg)
```

## 4、EnterpriseMessageQueue（企业级消息队列）

核心文件：`backend/modules/messaging/enterprise_queue.py`

### （1）核心特性

```
EnterpriseMessageQueue
├── 四级优先级队列
│   ├── URGENT (3) - 紧急消息
│   ├── HIGH   (2) - 高优先级
│   ├── NORMAL (1) - 默认
│   └── LOW    (0) - 低优先级
├── 消息去重（基于内容哈希）
├── 入站/出站分离
├── 死信队列（DLQ）
├── 自动重试（最多 3 次，降级优先级）
└── 监控指标
```

### （2）优先级调度

```python
# enterprise_queue.py:177-207
async def dequeue(self):
    """按优先级出队"""
    # 先检查高优先级队列
    for priority in [URGENT, HIGH, NORMAL, LOW]:
        queue = self._queues[priority]
        if not queue.empty():
            return await queue.get()

    # 所有队列都空，等待任意队列有消息
    tasks = [asyncio.create_task(queue.get()) for queue in self._queues.values()]
    done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_COMPLETED)
    for task in pending:
        task.cancel()
    return done.pop().result()
```

### （3）消息去重

```python
# enterprise_queue.py:251-270
def _hash_message(self, msg):
    """计算消息哈希"""
    content = f"{msg.channel}:{msg.chat_id}:{msg.sender_id}:{msg.content}"
    return hashlib.md5(content.encode()).hexdigest()

def _is_duplicate(self, msg_hash):
    """滑动窗口去重"""
    if msg_hash not in self._message_hashes:
        return False
    timestamp = self._message_hashes[msg_hash]
    if time.time() - timestamp > self._dedup_window:  # 默认 60 秒
        del self._message_hashes[msg_hash]
        return False
    return True
```

去重机制防止同一条消息被处理多次（如网络重传导致重复）。

### （4）失败重试与死信队列

```python
# enterprise_queue.py:236-249
async def mark_failed(self, msg, error):
    msg.retry_count += 1
    if msg.retry_count < msg.max_retries:
        # 降级优先级后重新入队
        lower_priority = MessagePriority(max(0, msg.priority.value - 1))
        await self._queues[lower_priority].put(msg)
    else:
        # 超过最大重试次数 → 进入死信队列
        await self._dead_letter_queue.put((msg, error))
```

重试策略：失败后降低优先级重试，确保不会因为一条消息的失败阻塞其他消息。

## 5、ChannelMessageHandler（频道消息处理器）

核心文件：`backend/modules/channels/handler.py`

### （1）消息处理主循环

```python
# handler.py:159-185
async def start_processing(self):
    """从消息总线消费入站消息"""
    consecutive_errors = 0
    while True:
        try:
            msg = await self.bus.consume_inbound()
            consecutive_errors = 0
            # 每条消息在独立的 asyncio Task 中处理
            asyncio.create_task(self.handle_message(msg))
        except Exception as e:
            consecutive_errors += 1
            if consecutive_errors >= 10:
                await asyncio.sleep(5)  # 连续错误过多，暂停恢复
```

注意：`asyncio.create_task()` 使每条消息并行处理，不会阻塞队列消费。

### （2）单条消息处理流程

```python
# handler.py:187-278
async def handle_message(self, msg):
    # 1. 清理 @mention 标记
    content = _AT_MENTION_RE.sub("", msg.content).strip()

    # 2. 限流检查
    if self.rate_limiter:
        allowed, error_msg = await self.rate_limiter.check(msg.sender_id)
        if not allowed:
            await self._send_reply(msg, error_msg)
            return

    # 3. 命令分发
    cmd = content.lower()
    if cmd in ("/new", "/newsession"):
        await self._handle_new_session_command(msg)
        return
    if cmd in ("/help", "/h", "/?"):
        await self._handle_help_command(msg)
        return
    # ... 其他命令

    # 4. 获取/创建会话
    session_id = await self._get_or_create_session(msg)

    # 5. 保存用户消息到数据库
    await self._save_message(session_id, "user", msg.content)

    # 6. 加载历史消息
    history = await self._get_session_history(session_id)

    # 7. Agent 处理
    response = await self._process_with_agent(
        session_id, msg.content, history, cancel_token,
        channel=msg.channel, chat_id=msg.chat_id,
    )

    # 8. 保存助手回复 + 发送
    if response:
        await self._save_message(session_id, "assistant", response)
        await self._send_reply(msg, response)
```

### （3）支持的命令

| 命令 | 功能 |
|------|------|
| `/new` | 创建新会话 |
| `/list` | 列出最近会话 |
| `/switch <id>` | 切换会话 |
| `/clear` | 清除当前会话历史 |
| `/stop` | 停止当前任务 |
| `/help` | 显示帮助 |

### （4）配置热重载

```python
# handler.py:107-142
def reload_config(self, provider=None, model=None, temperature=None, ...):
    """前端修改设置后调用，无需重启服务"""
    if provider is not None:
        self.agent_loop.provider = provider
    if model is not None:
        self.agent_loop.model = model
    if persona_config is not None:
        self.context_builder.persona_config = persona_config
```

这使得用户在前端修改模型或人设后，渠道处理器能立即生效。

### （5）友好错误转换

```python
# handler.py:31-44
def _friendly_channel_error(raw):
    """将技术错误转换为用户可读的提示"""
    if "429" in raw or "quota" in raw:
        return "AI 服务额度不足，请联系管理员检查 API 账户余额。"
    if "401" in raw or "api_key" in raw:
        return "API 认证失败，请联系管理员检查密钥配置。"
    if "timeout" in raw:
        return "网络连接异常，请稍后重试。"
    if "context length" in raw:
        return "对话上下文过长，请发送 /new 创建新会话后重试。"
    return "处理消息时出错，请稍后重试。"
```

## 6、RateLimiter（限流器）

核心文件：`backend/modules/messaging/rate_limiter.py`

采用**令牌桶算法**：
- 默认：10 次请求 / 60 秒
- 每个用户独立的令牌桶
- 令牌以恒定速率自动补充

## 7、各渠道实现特点

### （1）飞书（Feishu）详解

核心文件：`backend/modules/channels/feishu.py` + `backend/modules/channels/feishu_websocket_worker.py`

#### a. 架构：双进程模型

飞书渠道使用了**主进程 + WebSocket 子进程**的双进程架构：

```
┌─────────────────────────────────────────────────────────┐
│ 主进程（asyncio 事件循环）                                │
│                                                         │
│   FeishuChannel                                         │
│   ├── _read_ws_messages()  ← 从 Queue 读取消息           │
│   ├── _process_message()   ← 解析消息，调用 _handle_message│
│   ├── send()               ← 发送回复（卡片消息）          │
│   └── _download_image()    ← 下载用户发送的图片            │
│            ▲                                            │
│            │ multiprocessing.Queue                       │
│            ▼                                            │
│   ┌─────────────────────────────────────┐               │
│   │ 子进程 feishu-websocket-worker      │               │
│   │  FeishuWebSocketWorker              │               │
│   │  ├── lark.ws.Client (WebSocket连接) │               │
│   │  └── on_message() → Queue.put()    │               │
│   └─────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────┘
```

为什么需要子进程？因为飞书 SDK（`lark-oapi`）的 WebSocket 客户端 `lark.ws.Client.start()` 内部会创建自己的事件循环，与主进程的 asyncio 事件循环冲突。所以用 `multiprocessing.Process` 隔离到独立进程。

#### b. 启动流程

```python
# feishu.py:76-116
async def start(self):
    # 1. 创建飞书 API 客户端（用于发送消息、下载图片等）
    self._client = lark.Client.builder()
        .app_id(self.config.app_id)
        .app_secret(self.config.app_secret)
        .build()

    # 2. 创建进程间通信队列（最大容量 1000 条）
    ctx = get_context("spawn")
    self._message_queue = ctx.Queue(maxsize=1000)

    # 3. 启动 WebSocket 子进程
    self._ws_process = ctx.Process(
        target=run_worker,  # 子进程入口函数
        args=(app_id, app_secret, self._message_queue),
        daemon=True,
    )
    self._ws_process.start()

    # 4. 启动消息队列读取任务
    asyncio.create_task(self._read_ws_messages())

    # 5. 保持 start() 不返回（被 ChannelManager 的监督循环需要）
    while self._running:
        await asyncio.sleep(1)
```

注意第 5 步：`start()` 必须保持阻塞，因为 `ChannelManager._start_channel_supervised()` 用 `start()` 是否返回来判断渠道是否断开。

#### c. 消息接收流程

```
飞书服务器 --WebSocket--> 子进程 on_message()
    --> Queue.put(msg_data)
    --> 主进程 _read_ws_messages() 从 Queue 读取
    --> _process_message() 解析消息
    --> _handle_message() 鉴权 + 回调到 ChannelManager
```

子进程的 `on_message()` 将飞书 SDK 的事件对象转为普通 dict（因为跨进程通信只能传可序列化的数据）：

```python
# feishu_websocket_worker.py:50-68
def on_message(self, data):
    msg_data = {
        "type": "message",
        "message_id": message.message_id,
        "sender_id": sender.sender_id.open_id,
        "chat_id": message.chat_id,
        "chat_type": message.chat_type,  # "p2p" 或 "group"
        "msg_type": message.message_type, # "text" / "image" 等
        "content": message.content,       # JSON 字符串
    }
    self.message_queue.put_nowait(msg_data)
```

主进程用 `run_in_executor` 在线程池中读取 Queue（因为 `Queue.get()` 是阻塞操作，不能直接在 asyncio 中调用）：

```python
# feishu.py:153-174
async def _read_ws_messages(self):
    while self._running:
        msg_data = await asyncio.get_running_loop().run_in_executor(
            None, lambda: self._message_queue.get(timeout=1.0)
        )
        if msg_data and msg_data.get("type") == "message":
            await self._process_message(msg_data)
```

#### d. 消息去重

飞书可能因网络重传发送重复消息，用 `OrderedDict` 做去重缓存：

```python
# feishu.py:185-190
if message_id in self._processed_ids:
    return  # 已处理过，跳过
self._processed_ids[message_id] = None
while len(self._processed_ids) > 1000:
    self._processed_ids.popitem(last=False)  # 淘汰最早的
```

最多缓存 1000 个消息 ID，超出时淘汰最早的（FIFO）。

#### e. 消息类型处理

| 消息类型 | 处理方式 |
|---------|---------|
| `text` | 解析 JSON 提取文本内容 |
| `image` | 下载图片到本地，生成图片分析指令交给 Agent |
| 其他（`audio`/`file`/`sticker`） | 转为 `[语音]`/`[文件]`/`[表情]` 占位文本 |

图片消息的处理比较特殊 —— 会把图片下载到 `data/temp/images/` 目录，然后将内容替换为一条 Agent 可以执行的分析命令：

```python
content = f"用户发送了图片（{image_path}），请执行："
         f"python skills/image-analysis/scripts/vision.py "
         f'analyze --image {image_path} --prompt "描述图片内容"'
```

这样 Agent 会调用 `exec` 工具执行图片分析技能。

#### f. 表情回应

收到消息后会自动给消息添加一个 👍 表情回应，让用户知道 Bot 已收到消息：

```python
# feishu.py:197
await self._add_reaction(message_id, "THUMBSUP")
```

由于飞书 SDK 的 API 是同步的，用 `run_in_executor` 放到线程池执行，避免阻塞主事件循环。

#### g. 出站消息：卡片格式

回复消息不是纯文本，而是飞书**卡片消息**（`msg_type="interactive"`），支持 Markdown 渲染和表格：

```python
# feishu.py:389-408
def _build_card_elements(self, content):
    """将内容拆分为 markdown 段落 + 飞书表格元素"""
    # 用正则匹配 markdown 表格
    for m in self._TABLE_RE.finditer(content):
        # 表格前的文字 → markdown 元素
        # 表格本身 → 解析为飞书原生 table 元素
    # 剩余文字 → markdown 元素
```

这样 Agent 回复中包含的 Markdown 表格会在飞书中渲染为原生表格，而不是等宽字体文本。

#### h. 接收 ID 类型判断

发送消息时需要指定 `receive_id_type`：

```python
# feishu.py:421
receive_id_type = "chat_id" if msg.chat_id.startswith("oc_") else "open_id"
```

- `oc_` 开头 → 群聊 ID（`chat_id`）
- 其他 → 个人 ID（`open_id`）

#### i. 停止流程

```python
# feishu.py:118-147
async def stop(self):
    self._running = False
    # 1. 先 terminate 子进程
    self._ws_process.terminate()
    self._ws_process.join(timeout=5)
    # 2. 如果 5 秒内没结束，强制 kill
    if self._ws_process.is_alive():
        self._ws_process.kill()
    # 3. 清空并关闭消息队列
    self._message_queue.close()
```

### （2）钉钉（DingTalk）

- Stream 模式连接
- 消息重放支持
- 卡片消息格式

### （3）QQ

- 使用官方 qq-botpy SDK
- 可选腾讯云 OSS 媒体上传

### （4）Telegram

- Long polling 模式（无需 webhook）
- 支持自定义代理

## 8、对应代码阅读指引

| 阅读顺序 | 文件 | 重点关注 |
|----------|------|----------|
| 1 | `channels/base.py` 全文 | 消息数据结构和抽象接口 |
| 2 | `channels/manager.py` 全文 | 频道管理和监督重连 |
| 3 | `channels/handler.py` 全文 | 消息处理流程和命令分发 |
| 4 | `messaging/enterprise_queue.py` 全文 | 优先级队列和去重 |
| 5 | `messaging/rate_limiter.py` | 令牌桶限流 |
| 6 | `channels/feishu.py` | 了解一个具体渠道实现 |
| 7 | `channels/telegram.py` | 对比另一个渠道实现 |

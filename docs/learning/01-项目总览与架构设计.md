# 一、CountBot 项目总览与架构设计

## 1、项目简介

CountBot 是一个轻量级、可扩展的 AI Agent 框架，专为中文用户和国内 LLM 提供商优化。它实现了一个生产级的智能助手，具备记忆管理、多渠道支持和智能任务调度等核心能力。

### （1）核心定位

CountBot 不是一个简单的聊天机器人，而是一个完整的 **AI Agent 框架**。它的核心设计理念是：

- **Agent 而非 Chatbot**：不仅能对话，还能调用工具、执行命令、操作文件
- **多渠道统一**：一套代码同时服务 Web、飞书、钉钉、QQ、Telegram 等平台
- **记忆持久化**：不仅记住当前对话，还能跨会话记忆用户偏好和重要信息
- **主动关怀**：通过心跳系统主动问候用户，而不是被动等待指令

### （2）技术栈概览

| 层级     | 技术选型                    | 说明            |
| ------ | ----------------------- | ------------- |
| 后端框架   | FastAPI                 | 高性能异步 Web 框架  |
| 数据库    | SQLite + SQLAlchemy 2.0 | 异步 ORM，轻量级存储  |
| LLM 集成 | LiteLLM                 | 统一接口支持 40+ 模型 |
| 前端     | Vue 3 + TypeScript      | 预编译的 SPA 应用   |
| 实时通信   | WebSocket               | 流式响应推送        |
| 任务调度   | 自研精确调度器                 | 非轮询，按需唤醒      |

### （3）项目数据概览

| 指标 | 数值 |
|------|------|
| Python 文件数 | ~121 |
| 后端代码量 | ~22,700 行 |
| 数据库表 | 8 张 |
| 内置工具 | 13 个 |
| 支持渠道 | 6 个 |
| 技能插件 | 10 个 |
| 性格预设 | 12 种 |
| API 路由组 | 14 个 |

## 2、目录结构

### （1）顶层目录

```
CountBot/
├── backend/                  # 后端核心代码（本项目的学习重点）
├── frontend/                 # 前端 Vue 3 应用（预编译分发）
├── skills/                   # 技能插件目录
├── docs/                     # 项目文档
├── .github/                  # CI/CD 配置
├── start_app.py              # 生产环境启动入口
├── start_dev.py              # 开发环境启动入口
├── start_desktop.py          # 桌面应用启动入口
└── requirements.txt          # Python 依赖列表
```

### （2）后端核心目录

```
backend/
├── app.py                    # FastAPI 应用入口 ★ 学习起点
├── database.py               # 数据库连接配置
├── api/                      # REST API 路由层（14 个路由器）
│   ├── chat.py              # 聊天 API
│   ├── settings.py          # 设置 API
│   ├── auth.py              # 认证 API
│   ├── tools.py             # 工具 API
│   ├── memory.py            # 记忆 API
│   ├── skills.py            # 技能 API
│   ├── cron.py              # 定时任务 API
│   ├── channels.py          # 渠道 API
│   └── ...
├── models/                   # 数据库模型层
│   ├── session.py           # 会话模型
│   ├── message.py           # 消息模型
│   ├── cron_job.py          # 定时任务模型
│   ├── personality.py       # 性格模型
│   └── ...
├── modules/                  # 核心业务逻辑层 ★ 最重要的目录
│   ├── agent/               # Agent 核心（循环、记忆、上下文）
│   ├── auth/                # 认证与安全
│   ├── channels/            # 多渠道支持
│   ├── config/              # 配置管理
│   ├── cron/                # 任务调度
│   ├── messaging/           # 消息队列与限流
│   ├── providers/           # LLM 提供商
│   ├── session/             # 会话管理
│   ├── system/              # 系统工具
│   └── tools/               # 工具注册与实现
├── utils/                    # 通用工具函数
│   ├── logger.py            # 日志配置
│   ├── paths.py             # 路径管理
│   └── network.py           # 网络工具
└── ws/                       # WebSocket 连接处理
    ├── connection.py         # 连接管理
    └── events.py             # 事件处理
```

### （3）技能插件目录

```
skills/
├── baidu-search/            # 百度搜索 + AI 生成
├── cron-manager/            # 定时任务管理 UI
├── email/                   # QQ/163 邮件管理
├── image-analysis/          # 图像分析（智谱/通义千问）
├── image-gen/               # 图像生成（ModelScope）
├── map/                     # 高德地图路线规划
├── news/                    # 新闻聚合（RSS）
├── weather/                 # 天气查询
├── web-design/              # HTML 生成 + Cloudflare 部署
└── agent-browser/           # 浏览器自动化
```

## 3、整体架构设计

![[canvas/CountBot-Architecture.excalidraw.md|2000]]

### （1）分层架构

CountBot 采用经典的三层架构，但在每一层都有独特的设计：

```
┌─────────────────────────────────────────────────────────┐
│                    接入层 (Access Layer)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ WebSocket│ │  飞书    │ │  钉钉    │ │ Telegram │    │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘    │
│       └──────┬─────┴──────┬─────┴──────┬─────┘           │
│              ▼            ▼            ▼                  │
│       EnterpriseMessageQueue (企业级消息队列)              │
│  ┌──────────────────────────────────────────────────┐    │
│  │ 四级优先级 │ 消息去重 │ 死信队列 │ 自动重试        │    │
│  └──────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│                  业务层 (Business Layer)                   │
│  ┌──────────────────────────────────────────────────┐    │
│  │              Agent Loop (Agent 主循环)             │    │
│  │  ┌─────────┐  ┌──────────┐  ┌──────────────┐    │    │
│  │  │ Context │  │ Provider │  │ ToolRegistry │    │    │
│  │  │ Builder │  │ (LiteLLM)│  │  (13 工具)    │    │    │
│  │  └─────────┘  └──────────┘  └──────────────┘    │    │
│  └──────────────────────────────────────────────────┘    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 记忆系统 │ │ 技能系统 │ │ 心跳服务 │ │ Cron调度 │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
├─────────────────────────────────────────────────────────┤
│                  数据层 (Data Layer)                       │
│  ┌──────────────────────────────────────────────────┐    │
│  │        SQLite + SQLAlchemy Async ORM              │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐  │    │
│  │  │Session │ │Message │ │CronJob │ │Personality│  │    │
│  │  └────────┘ └────────┘ └────────┘ └──────────┘  │    │
│  └──────────────────────────────────────────────────┘    │
│  ┌──────────────────────────────────────────────────┐    │
│  │        文件存储 (MEMORY.md, 审计日志, 配置)        │    │
│  └──────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

### （2）核心设计模式

#### （2.1）共享组件模式

这是 CountBot 最重要的设计决策之一。WebSocket 和渠道处理器共享同一套组件：

```python
# backend/app.py 中的 _create_shared_components()
shared = {
    "provider": provider,          # LLM 提供商（共享实例）
    "context_builder": context_builder,  # 上下文构建器（共享）
    "tool_registry": tool_registry,      # 工具注册表（每连接独立）
    "subagent_manager": subagent_manager, # 子代理管理器（共享）
    "memory": memory,                     # 记忆存储（共享）
    "skills": skills,                     # 技能加载器（共享）
}
```

这样做的好处：
- 所有渠道和 Web UI 使用完全一致的提示词、技能和工具
- 避免重复创建 LLM provider 实例，节约 API 配额
- 记忆和技能跨渠道共享

#### （2.2）依赖注入模式

组件在启动时创建，通过构造函数注入到需要它们的模块中：

```
app.py (lifespan)
  ├── 创建 Provider → 注入到 AgentLoop、HeartbeatService
  ├── 创建 ContextBuilder → 注入到 AgentLoop、ChannelMessageHandler
  ├── 创建 ToolRegistry → 注入到 AgentLoop
  └── 创建 ChannelManager → 注入到 ChannelMessageHandler
```

#### （2.3）策略模式

- `BaseChannel` 抽象基类定义统一接口，每个渠道实现不同的连接策略
- `Tool` 抽象基类定义工具接口，每个工具实现不同的执行逻辑
- 性格系统通过选择不同预设来改变 AI 的行为策略

#### （2.4）观察者模式

- 渠道通过回调函数将入站消息传递给消息队列
- WebSocket 连接管理器维护会话到连接的映射，支持按会话推送

## 4、核心数据流

### （1）用户消息处理全流程

```
用户输入（Web/飞书/钉钉/Telegram/QQ）
    │
    ▼
┌──────────────────────────────┐
│ 接入层：渠道接收 / WebSocket  │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 消息队列：优先级排序 + 去重    │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ 消息处理器：命令识别 + 限流    │
│ (/new, /help, /stop 等命令)  │
└──────────┬───────────────────┘
           │ 非命令消息
           ▼
┌──────────────────────────────┐
│ 会话管理：获取/创建会话        │
│ 加载历史消息                  │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────┐
│              Agent Loop (ReAct 循环)                   │
│                                                       │
│  1. ContextBuilder.build_system_prompt()              │
│     ├─ 核心身份 + 性格设定                             │
│     ├─ 已激活技能                                      │
│     └─ 可用技能摘要                                    │
│                                                       │
│  2. ContextBuilder.build_messages()                   │
│     ├─ system prompt                                  │
│     ├─ 历史消息                                       │
│     └─ 当前用户消息                                    │
│                                                       │
│  3. LiteLLMProvider.chat_stream() → 调用 LLM          │
│                                                       │
│  4. 解析响应                                           │
│     ├─ 有文本内容 → 流式输出给用户                      │
│     └─ 有工具调用 → 执行工具 → 将结果加入消息 → 回到 3  │
│                                                       │
│  5. 循环直到：                                         │
│     ├─ LLM 不再调用工具（正常结束）                      │
│     ├─ 达到最大迭代次数                                 │
│     └─ 用户取消                                        │
└──────────────────────────────┬────────────────────────┘
                               │
                               ▼
┌──────────────────────────────┐
│ 响应输出：                    │
│ Web → WebSocket 流式推送      │
│ 渠道 → 出站消息队列 → 渠道发送 │
└──────────────────────────────┘
```

### （2）定时任务执行流程

```
CronScheduler 计算最近的任务时间
    │
    ▼ asyncio.sleep(精确等待)
    │
    ▼
CronScheduler._on_timer() 触发
    │
    ▼ 获取所有到期任务
    │
    ▼ 通过 Semaphore 控制并发（最多 3 个）
    │
    ▼
CronExecutor.execute()
    ├── 心跳任务 → HeartbeatService.execute() → 生成问候语
    └── 普通任务 → AgentLoop.process_direct() → Agent 处理
    │
    ▼ 如果任务指定了渠道
    │
    ▼
ChannelManager.send_message() → 发送到指定渠道
```

## 5、学习路线建议

建议按以下顺序阅读本系列文档：

| 序号  | 文档            | 重点内容              | 对应代码                                      |
| --- | ------------- | ----------------- | ----------------------------------------- |
| 01  | 项目总览与架构设计（本文） | 全局理解              | -                                         |
| 02  | 应用启动与生命周期     | 启动流程、组件创建         | `app.py`                                  |
| 03  | Agent 核心系统    | ReAct 循环、上下文构建、记忆 | `modules/agent/`                          |
| 04  | 工具系统设计        | 工具基类、注册表、内置工具     | `modules/tools/`                          |
| 05  | 渠道与消息系统       | 多渠道架构、消息队列        | `modules/channels/`, `modules/messaging/` |
| 06  | 定时调度与心跳       | 精确调度、主动问候         | `modules/cron/`, `agent/heartbeat.py`     |
| 07  | 安全与认证         | 零配置安全、远程认证        | `modules/auth/`                           |
| 08  | 技能插件与配置       | 插件机制、配置管理         | `modules/config/`, `agent/skills.py`      |
| 09  | 数据库与数据模型      | ORM 设计、异步操作       | `database.py`, `models/`                  |
| 10  | 代码阅读指南        | 按模块的逐文件阅读顺序       | 全部                                        |

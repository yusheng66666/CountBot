# 十一、定时调度与心跳系统

定时调度系统让 CountBot 能够按时执行任务，心跳系统让 AI 能够主动关怀用户。

## 1、Cron 系统架构

```
┌──────────────────────────────────────────────┐
│                CronScheduler                  │
│            (智能调度器 - 按需唤醒)              │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │ 计算最早任务时间 → sleep(精确等待)        │  │
│  │ → 到点唤醒 → 获取到期任务 → 并发执行     │  │
│  └─────────────┬───────────────────────────┘  │
│                │                               │
│                ▼                               │
│  ┌─────────────────────────────────────────┐  │
│  │ CronExecutor (任务执行器)                 │  │
│  │ ├── 心跳任务 → HeartbeatService          │  │
│  │ └── 普通任务 → AgentLoop.process_direct  │  │
│  └─────────────────────────────────────────┘  │
│                │                               │
│                ▼                               │
│  ┌─────────────────────────────────────────┐  │
│  │ CronService (数据访问层)                   │  │
│  │ ├── list_jobs() / get_due_jobs()          │  │
│  │ ├── calculate_next_run()                   │  │
│  │ └── update_job_status()                    │  │
│  └─────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

## 2、CronScheduler（智能调度器）

核心文件：`backend/modules/cron/scheduler.py`

### （1）核心设计理念：精确唤醒而非轮询

传统的 cron 实现通常每秒或每分钟检查一次是否有任务到期，这会浪费 CPU 资源。CountBot 的调度器采用**精确计算唤醒时间**的方式：

```python
# scheduler.py:124-160
def _arm_timer(self):
    """调度下一个定时器"""
    async def schedule_next():
        # 1. 获取最近的任务运行时间
        next_wake = await self._get_next_wake_time()

        if not next_wake:
            await asyncio.sleep(60)  # 没有任务，60秒后重新检查
            self._arm_timer()
            return

        # 2. 计算需要等待的精确时间
        now = _now_shanghai()
        delay = (next_wake - now).total_seconds()
        if delay < 0:
            delay = 0  # 已过期，立即执行

        # 3. 精确等待
        await asyncio.sleep(delay)

        # 4. 到点执行
        if self._running:
            await self._on_timer()

    self._timer_task = asyncio.create_task(schedule_next())
```

### （2）并发控制

```python
# scheduler.py:26-40
DEFAULT_MAX_CONCURRENT = 3  # 最大并发执行数
DEFAULT_JOB_TIMEOUT = 300   # 单任务最大执行时间（5分钟）

class CronScheduler:
    def __init__(self, ...):
        self._semaphore = asyncio.Semaphore(max_concurrent)  # 并发信号量
        self._active_jobs: set[str] = set()   # 正在执行的 job_id 集合
        self._active_tasks: set[asyncio.Task] = set()  # 正在执行的 Task
```

### （3）安全执行包装

```python
# scheduler.py:201-240
async def _execute_job_safe(self, job):
    """带信号量、超时和独立 session 的安全执行"""
    async with self._semaphore:          # 并发控制
        self._active_jobs.add(job.id)
        try:
            async with self.db_session_factory() as db:  # 独立数据库会话
                service = CronService(db)
                fresh_job = await service.get_job(job.id)  # 重新加载最新状态
                if fresh_job and fresh_job.enabled:
                    await asyncio.wait_for(     # 超时控制
                        self._execute_job(fresh_job, service),
                        timeout=self.job_timeout,
                    )
        except asyncio.TimeoutError:
            # 更新超时状态
            ...
        finally:
            self._active_jobs.discard(job.id)
```

关键点：
- 每个任务使用**独立的数据库会话**，避免事务冲突
- 执行前**重新加载任务**，获取最新状态（可能已被禁用）
- **信号量**限制并发数（默认最多 3 个任务同时执行）
- **超时控制**防止任务无限运行

### （4）防重复执行

```python
# scheduler.py:162-199
async def _on_timer(self):
    """定时器触发时"""
    due_jobs = await service.get_due_jobs()

    # 过滤掉正在执行的任务
    pending = [j for j in due_jobs if j.id not in self._active_jobs]
    if not pending:
        return
```

### （5）SQLite 并发锁处理

```python
# scheduler.py:300-313
async def _safe_commit(self, db):
    """带重试的安全 commit"""
    for attempt in range(MAX_COMMIT_RETRIES):  # 最多 3 次
        try:
            await db.commit()
            return
        except Exception as e:
            if "database is locked" in str(e).lower():
                wait = 0.5 * (attempt + 1)  # 0.5s → 1.0s → 1.5s
                await asyncio.sleep(wait)
            else:
                raise
```

## 3、CronExecutor（任务执行器）

核心文件：`backend/modules/cron/executor.py`

### （1）执行入口

```python
async def execute(self, job_id, message, channel, chat_id, deliver_response):
    # 1. 识别心跳任务
    if message == "__heartbeat__":
        greeting = await self.heartbeat_service.execute()
        if greeting and channel and chat_id:
            # 通过渠道发送问候语
            await self.channel_manager.send_message(OutboundMessage(...))
        return greeting

    # 2. 普通任务 → Agent 处理
    response = await self.agent.process_direct(
        content=message,
        session_id=f"cron:{job_id}",
    )

    # 3. 如果指定了渠道且需要投递响应
    if deliver_response and channel and chat_id and response:
        await self.channel_manager.send_message(OutboundMessage(...))

    return response
```

### （2）心跳任务与普通任务的区别

| | 心跳任务 | 普通任务 |
|---|---------|---------|
| 触发消息 | `__heartbeat__` | 任意文本 |
| 执行方式 | HeartbeatService.execute() | AgentLoop.process_direct() |
| 是否总是执行 | 否（有多重条件判断） | 是 |
| 是否使用工具 | 否（纯 LLM 生成） | 是 |

## 4、HeartbeatService（心跳服务）

核心文件：`backend/modules/agent/heartbeat.py`

### （1）主动问候的判断流程

```
execute() 入口
    │
    ├── 1. 免打扰时段检查
    │   └── 当前北京时间是否在 quiet_start ~ quiet_end 范围内？
    │       （默认 21:00 ~ 08:00 免打扰）
    │       → 是 → 跳过
    │
    ├── 2. 今日已达上限检查
    │   └── 今天已问候次数 >= max_greets_per_day？
    │       （默认每天最多 2 次）
    │       → 是 → 跳过
    │
    ├── 3. 用户空闲检测
    │   └── 查询所有会话中用户最近一条消息的时间
    │       空闲时间 < idle_threshold_hours？
    │       （默认 4 小时）
    │       → 不够空闲 → 跳过
    │
    ├── 4. 随机概率
    │   └── random.random() > 0.5？
    │       → 跳过（让问候时间分布更自然）
    │
    └── 5. 生成问候语
        └── 使用 LLM 生成个性化问候
```

### （2）问候语生成

```python
# heartbeat.py:161-223
async def _generate_greeting(self, now, idle_hours):
    # 构建时间描述
    hour = now.hour
    if hour < 12:
        time_desc = f"上午{hour}点"
    elif hour < 14:
        time_desc = f"中午{hour}点"
    # ...

    # 读取最近记忆作为上下文
    memory = MemoryStore(self.workspace / "memory")
    recent = memory.get_recent(5)

    # 获取性格描述
    personality_desc = get_personality_prompt(self.personality, ...)

    # 调用 LLM 生成
    prompt = HEARTBEAT_GREETING_PROMPT.format(
        ai_name=self.ai_name,
        user_name=self.user_name,
        time_desc=time_desc,
        idle_hours=idle_hours,
        personality_desc=personality_desc,
        memory_context=memory_context,
    )
    # ...
```

问候语会结合：
- 当前时间（上午/中午/下午/晚上）
- 用户空闲时长
- AI 的性格设定
- 最近的记忆内容

### （3）免打扰时段（跨午夜支持）

```python
# heartbeat.py:73-83
def _is_quiet_hour(self, hour):
    if self.quiet_start <= self.quiet_end:
        # 不跨午夜：如 1:00-6:00
        return self.quiet_start <= hour < self.quiet_end
    else:
        # 跨午夜：如 22:00-8:00
        return hour >= self.quiet_start or hour < self.quiet_end
```

### （4）内置心跳 Cron Job 的自动创建

```python
# heartbeat.py:234-307
async def ensure_heartbeat_job(db_session_factory, heartbeat_config=None):
    """应用启动时确保心跳 cron job 存在"""
    # 1. 查询是否已存在
    existing = await service.get_job(HEARTBEAT_JOB_ID)  # "builtin:heartbeat"

    # 2. 已存在 → 同步配置
    if existing:
        if existing.enabled != enabled:
            existing.enabled = enabled
        # ... 同步 channel, chat_id, schedule 等

    # 3. 不存在 → 创建
    else:
        job = CronJob(
            id=HEARTBEAT_JOB_ID,
            name="系统问候（内置）",
            schedule="0 * * * *",       # 每小时整点
            message="__heartbeat__",    # 特殊标记
            enabled=enabled,
            deliver_response=True,
        )
        db.add(job)
```

## 5、CronService（数据访问层）

核心文件：`backend/modules/cron/service.py`

### （1）核心方法

```python
class CronService:
    async def list_jobs(self, enabled_only=False)     # 列出任务
    async def get_job(self, job_id)                    # 获取单个任务
    async def create_job(self, name, schedule, ...)    # 创建任务
    async def update_job(self, job_id, ...)            # 更新任务
    async def delete_job(self, job_id)                 # 删除任务
    async def get_due_jobs()                           # 获取到期任务
    def calculate_next_run(self, schedule, base_time)  # 计算下次运行时间
```

### （2）下次运行时间计算

使用 `croniter` 库解析 cron 表达式并计算：

```python
from croniter import croniter

def calculate_next_run(self, schedule, base_time=None):
    base = base_time or _now_shanghai()
    cron = croniter(schedule, base)
    return cron.get_next(datetime)
```

## 6、定时任务的完整生命周期

```
1. 用户创建任务（通过 API 或 AI 工具调用）
    │
    ▼
2. CronService.create_job() → 保存到数据库
    │
    ▼
3. CronScheduler.trigger_reschedule() → 重新计算所有任务时间
    │
    ▼
4. CronScheduler._arm_timer() → 设置精确定时器
    │
    ▼ asyncio.sleep(精确等待)
    │
    ▼
5. CronScheduler._on_timer() → 获取到期任务
    │
    ▼
6. CronScheduler._execute_job_safe() → 带并发控制和超时保护
    │
    ▼
7. CronExecutor.execute() → 区分心跳/普通任务
    │
    ├── 心跳 → HeartbeatService → 生成问候 → 渠道发送
    │
    └── 普通 → AgentLoop → 执行任务 → 渠道发送（如配置）
    │
    ▼
8. 更新任务状态（last_run, last_status, run_count, next_run）
    │
    ▼
9. CronScheduler._arm_timer() → 为下次执行设置定时器
```

## 7、对应代码阅读指引

| 阅读顺序 | 文件 | 重点关注 |
|----------|------|----------|
| 1 | `cron/scheduler.py` 全文 | 精确唤醒和并发控制 |
| 2 | `cron/executor.py` | 心跳/普通任务分发 |
| 3 | `cron/service.py` | 数据库操作和 cron 解析 |
| 4 | `agent/heartbeat.py` 全文 | 主动问候的判断逻辑 |
| 5 | `agent/prompts.py` | HEARTBEAT_GREETING_PROMPT 模板 |
